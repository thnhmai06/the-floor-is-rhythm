#include "structures/events/action/audio.h" // Header
#include "utilities.hpp"

namespace Structures::Events::Action::Audio
{
	/*AudioAction::AudioAction(const OsuParser::Beatmap::Objects::Event::Objects::SampleObject& osu_sample)
		: layer(static_cast<Types::Game::Gameplay::Layer>(osu_sample.layer_num)), file_path(osu_sample.filepath),
		volume(std::round(Utilities::Math::to_value(static_cast<float>(osu_sample.volume) / 100.0f, 0, MIX_MAX_VOLUME)))
	{
		start_time = end_time = osu_sample.time;
	}*/
}
